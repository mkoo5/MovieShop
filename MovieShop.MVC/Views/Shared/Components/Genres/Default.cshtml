@model IEnumerable<ApplicationCore.Models.Response.GenreModel>

@foreach (var genre in Model)
{
<a asp-controller="Movies" asp-action="Genre" asp-route-id="@genre.Id" class="dropdown-item"> @genre.Name </a>
}
